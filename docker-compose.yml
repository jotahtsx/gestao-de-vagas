services:
  postgres:
    container_name: job_management_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=sexta
      - POSTGRES_PASSWORD=feira
      - POSTGRES_DB=job_management
